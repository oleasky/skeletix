import React from "react";
interface ModalButtonProps {
    content: React.ReactNode;
    children: React.ReactNode;
    className?: string;
}
export declare const ModalButton: ({ content, children, className }: ModalButtonProps) => React.JSX.Element;
export {};
